/** Create a function that accepts an array and returns the total
 *  Examples:
    addItems([1,5,6]) // 12
    addItems([1,-2,-3]) // -5
 * 
 *
 */
const addItems = arr => {};

/*** Create a function that accepts an array and returns an object  with  key displaying them name of item and the value of
 * of the item
 **
 */

const generateTally = arr => {};

/** create a function that accepts an array and returns a f
 * lattened array
 *
 */

const flattenArray = arr => {};

/** convert an array of objects  to an object and the object key should be accesible by id */
const arrayToObject = arr => {};

module.exports = {
  addItems,
  generateTally,
  flattenArray,
  arrayToObject
};
